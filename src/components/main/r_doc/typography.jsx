import "../../../styles/main/r_doc/typography.scss"

const STR__CURR_ROUTE = "r_doc";
const STR__COMPONENT = "typography";

const DocTypography = () => {
  return (
    <div className={`${STR__COMPONENT} ${STR__CURR_ROUTE}`}>

    </div>
  );
};

export default DocTypography;