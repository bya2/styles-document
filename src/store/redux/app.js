import { createStore } from "redux";

function reducer__App(state = {}, action) {
  switch (action.type) {
    default:
      break;
  }

  return state;
}

export const store__App = createStore(reducer__App);
