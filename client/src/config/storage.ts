import { I_map } from "@/models/reusables";

export const local_storage_keys__map: I_map<string> = {
  visited_pages: "ls-visited-pages",

  ls__sds_auth_ref: "ls-sds-auth-ref",
}

export const session_storage_keys__map: I_map<string> = {
  ss__sds__auth_ref: "ss__sds__auth_ref",
}