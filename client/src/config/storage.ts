import { I_map } from "@/models/reusables";

export const local_storage_keys__map: I_map<string> = {
  visited_pages: "ls-visited-pages",
}

export const session_storage_key__map: I_map<string> = {

}